const validator = {
  
}

/* const validateButton = document.getElementById("btn-validate");
const cardNum = document.getElementById("cardumber"); */

const meuArray = [1, 2, 5, 7, 9, 4];
const meuArrayInvertido = meuArray.slice(0).reverse();


if (meuArrayInvertido.length % 2 === 0) {
    
} else {
    
}

// export default validator;